# LinkedIn approval publishing — September 16, 2026

Don confirmed the contract: suggest a comment in Approvals; approve publishes it; reject does not publish. Daily weekday 7 AM discovery remains, with duplicate suppression rather than a lower-frequency workaround.

Root cause: the installed linkedin-posting skill instructed workers to create review-only comments for Don to paste manually. The accepted tasks dispatched successfully, but workers completed after preparing text, without a posting receipt. One AI-dangers task also contained an unrelated AI-search draft. Legacy review approvals must not be replayed automatically.

The managed skill and SOUL execution rule now require publish envelopes with verified structured LinkedIn targets. scripts/linkedin-comments.py creates unassigned approvals, checks existing structured approvals and a private persistent SQLite ledger, verifies the connected posting identity, publishes the exact persisted approved payload, and accepts only a matching comment receipt. Failed or ambiguous writes remain unconfirmed and prevent blind duplicates; workers must block rather than complete them. Duplicate pending or rejected suggestions are skipped too. Manual-paste fallback is removed.

The connected account is ACTIVE and LINKEDIN_GET_MY_INFO returned the expected identity. Composio exposes LINKEDIN_CREATE_COMMENT_ON_POST. Its schema accepts share/ugcPost URNs, not activity URNs. Discovery must resolve actual source metadata, not guess a URN from a URL. Account access to individual third-party posts and actual comment publication remain to be demonstrated by a correctly targeted approved suggestion. The helper does not infer permission from the existence of a tool.

Managed instructions and executor are installed by scripts/install-linkedin-workflow.py, also invoked during service installation. Existing files are backed up privately under HERMES_HOME/eaios/workflow-backups. State persists under HERMES_HOME/eaios/linkedin-comments.db. Scan instructions also check legacy author/title records because old envelopes lack structured IDs. This can prevent repeated suggestions created by EAiOS; detecting comments made outside EAiOS depends on permitted LinkedIn reads.

Validation: ten publishing guard tests, 300 application tests, and 29 updater/release tests pass. One full application run hit a transient optimistic-chat-node assertion; the repeated complete run passed. Tests cover approval/rejection, exact edited payload, target/receipt mismatch, connected identity, decision changes during preparation, uncertain network outcomes, and duplicate suggestions/writes. No public comments were posted during validation. Publish a new application release before provisioning another box with these changes; v0.1.0 predates this fix.
