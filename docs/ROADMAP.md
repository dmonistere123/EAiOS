# EAiOS — Build Roadmap (living document)

**Purpose:** the single source of truth for what's built, what's deferred, and
why. HANDOFF.md carries session state and gotchas; THIS file carries scope
decisions. Update it every phase — an item that isn't here doesn't exist.

**Governance invariants (never defer these):** pages talk only to typed
adapters · mock-first per slice with live fallback · no secrets in the client
bundle · external writes gate on approvals · honest degradation over fake
success (spec §2) · tests green before commit.

## First-box release priority — September 16, 2026

Weekly stable GitHub Release discovery, manual checks, confirmed staged installation, persistent data paths, online backups, and verified service recovery are implemented and tested. First stable release publication and a deployed-box release smoke test remain shipping gates. Central fleet inventory, remote rollout controls, and automatic release/backup retention remain future work.

## Phase status

| Phase | Scope | Status |
|---|---|---|
| 0 | Integration matrix, repo, design tokens | ✅ |
| 1 | Executive shell, 11 routes, mock adapters | ✅ |
| 2 | Live adapter (agents, cron, activity) + seq-guarded runtime | ✅ |
| 3 | Governed work loop: kanban WorkItems, approvals, policies (§9) | ✅ |
| 4 | Schedule (live cron overlay + create), Connections (Composio) | ✅ |
| 5 | Skills, Knowledge/RAG + citations, Playbooks, acceptance | ✅ 2026-08-26 |
| 6 | Usage, Assistant (live chat + citation store), Artifacts, Env files, Agent factory | ✅ 2026-08-27 |
| 7 | Packaging for other CEOs' boxes (prod server, systemd, installer) | ◻ future |

## Deferred-items register

Every known "not yet", with rationale and target. Add items here the moment
they're noticed — never leave them as tribal knowledge.

| # | Item | Why deferred | Target |
|---|---|---|---|
| F1 | ~~Playbook create/edit/new-version UI~~ **DONE 2026-08-28 (W7)** — editor drawer → confined PUT via `/api/playbooks-index` (`app/server/authoring.ts`); server-side patch bump; published edits land as new drafts. Remainder → F20 | — |
| F2 | **Skill enable/disable from EAiOS** | Hermes owns skill lifecycle (`skills.disabled` config); exec-UI toggle needs a config-write path + policy check. Decision needed: read-only library vs. exec control. | Phase 6 (decision), 7 (build) |
| F3 | ~~Skill authoring from EAiOS~~ **SUPERSEDED by D-B3 (2026-08-27) → create DONE 2026-08-28 (W7)** — D-B3 locked full in-app editing for user-created skills; W7 shipped the create drawer → confined POST `/api/skill-create` (slug/category validation, overwrite refused). Edit/delete → F21 | — |
| F4 | **Swarm playbook end-to-end run** | Needs a real competitor target + executive consent (spawns real agent work); verified only up to graph creation. | First real use |
| F5 | **Executive calendar live** | Needs user's one-time Google OAuth consent. | User action |
| F6 | **Composio app linking — NARROWED 2026-08-28 (W4)** | Managed auth configs proved API-creatable; W4's connect flow (auth config → Connect Link → hosted sign-in) ships in-app. Dashboard/custom auth config still needed only for bring-own-auth toolkits. | User action (bring-own-auth only) |
| F7 | **Morning briefing cron → Ally's Portal (7am)** | Probe 2026-08-28: the job EXISTS ("Morning Breifing", weekdays 7am, telegram deliver, last run OK, created 2026-08-26) — consent apparently given. Confirm with user; leave untouched meanwhile. | Confirm with user |
| F8 | ~~Answer→chunk citation store~~ **DONE 2026-08-27 (6.4b)** — decision: the persisted chat message IS the record (no separate link store); `parseCitations` renders chips on Ally's answers → shared ChunkDrawer with real chunk text; end-to-end clickable, tested | — |
|| F9 | ~~hermes serve + sidecar as systemd services~~ **DONE 2026-08-29 (8.2)** — user-mode units (Linger already on): `eaios-hermes-serve`, `eaios-knowledge-sidecar`, `eaios-server` from templates `install/systemd/*.tpl` rendered by `scripts/install-systemd-user.sh` (idempotent, validates Node ≥24 — doubles as the 8.3 installer's systemd step). Live swap verified: kill -9 → Restart=on-failure resurrected :5200 in ~4s; all WS paths green (direct 9119, via 5200 tunnel, via 5173 vite); vite dev untouched. Reboot-start **PROVEN 2026-08-29** — unplanned power-cycle (breaker): all three units self-recovered at boot; only the manual vite dev process stayed down (case for an `eaios-vite-dev` unit) | — |
|| F10 | ~~Prod discovery for skills/playbooks index~~ **DONE 2026-08-29 (8.1)** — zero-dep prod server `app/server/prod.ts` (Node ≥24 type-stripped): static dist + SPA fallback, all /api endpoints via shared `server/apiCore.ts` + `server/httpApi.ts`, /api/ws WS tunnel (token re-stamp + origin strip), /knowledge-api + /composio-api HTTP proxies (keys server-side), env-var config, `npm start` on :5200; 12-test hermetic suite `prod-server.test.ts`; live-verified (skills/usage identical to vite, WS profiles.list round-trip, traversal contained). vite consolidation → F22 | — |
| F11 | **Vectors/semantic retrieval** | FTS5-first decision locked 2026-08-26; vectors slot behind the same sidecar HTTP surface. | When FTS5 recall proves insufficient |
| F12 | **Preview-pane click harness flaky** | Hermes desktop delta engine loses sync; verify via vitest interaction tests instead. | External (Hermes) |
| F13 | **Retrieval enforcement for connector sources** | KnowledgeSource type 'connector' exists but no ingestion path (Composio-linked docs). | Phase 7+ |
| F14 | ~~Agent factory~~ **DONE 2026-08-27 (6.5)** — model.options grouped catalog + profiles.create (mirror_credentials default) behind `createAgent`; Staff → Add Agent drawer (slug validation, optgroup picker, SOUL seed); updateAgentConfig live for model writes (catalog-validated → profiles.configure); verified live (create/configure/delete round-trip) | — |
| F15 | ~~Usage budget config~~ **DONE 2026-08-26 (6.2)** — no host config-write RPC, so EAiOS-owned `~/eaios/settings.local.json` (gitignored) behind `/api/eaios-settings` middleware with a server-side key allowlist (`usageBudgetUsd`); `setUsageBudget` adapter method; Usage page inline budget editor | — |
| F16 | **Usage range picker** | 6.1 ships month-to-date only (fixture contract). Range UI (week/quarter/custom) is page polish once the slice is proven live. | Phase 7 polish |
| F17 | **Artifact attach-from-UI upload** | Needs multipart middleware + `kanban attach`; 6.3 shipped list/preview/download/share. Executives can attach via chat today. | Phase 7 |
| F18 | **Per-artifact archive + versioning** | Host has only destructive `attach-rm` and keeps no version history; per-artifact archive would need an EAiOS-side state store. | Phase 7+ / host feature |
| F19 | **Post-approval share execution** | 6.3 share creates the governed approval (decision record); actually sending after approval is the agent's work and needs an executor convention. | First real share |
| F20 | **Playbook duplicate / archive / safe-mode Test Run** | W7 shipped create/edit/versioning; these lifecycle actions remain (rest of old F1). | Phase 7 polish |
|| F21 | **Skill edit/delete in-app** | W7 scoped skills to create-only (plan); D-B3's "full editor" completes here. Needs the same confined-write pattern + skills.manage has no update RPC, so middleware-owned. | Phase 7 polish |
|| F22 | **vite.config middleware → delegate to apiCore/httpApi** | 8.1 extracted the shared core (`server/apiCore.ts`, `server/httpApi.ts`) but vite.config.ts kept its inline copies so Don's demo path stayed untouched (he demos from vite :5173 until the prod flip). Consolidation = delete ~450 lines from vite.config and delegate each middleware to httpApi handlers — do it when no demo is imminent, verify with the existing suites. /api/kanban already rides the shared router on both servers (2026-08-29 dogfood fix). | Phase 8 cleanup (pre-flip) |
|| F23 | ~~Worker-run transcript surfacing~~ **DONE 2026-08-29** — probed: kanban worker sessions (state.db source='kanban') resume+history fine despite the session.list deny-list; `/api/kanban-runs` (ATTACH state.db, title-convention join) → `listDelegatedRuns` adapter → Assistant rail "Delegated runs" section + Schedule completed-24h click-to-read, both opening `DelegatedRunDrawer` (result + real transcript); worker sessions also MERGED into Conversations — Ally tagged `kanban` (Don: "they truly are Ally sessions"); live-verified on :5173 (t_926a5b85's 22-message transcript incl. a telegram blocked by the security scanner for emoji — explains other "missing" telegrams) | — |
|| F24 | **Per-profile worker-session transcripts (specialist runs)** | `/api/kanban-runs` joins only the DEFAULT profile's state.db; specialist agents' worker sessions live in `profiles/<slug>/state.db`, so in a specialist's channel view their runs list in Delegated runs but WITHOUT transcripts (honest gap — no fake sessions). Fix: extend the join to ATTACH each profile's state.db (or union per-profile reads) — flagged by Don 2026-08-29. | Post-beta |
|| F25 | **Continue-chat on completed delegated runs** (Don-approved 2026-08-29; spike `docs/spike-run-resume-chat-2026-08-29.md`, t_95e162a0) | Drawer transcripts are read-only by D-B1. Probe: resume/history/prompt.submit ALL accept kanban sessions (deny-list only filters session.list); adapter lanes ~80% exist. Scope v1 = Ally-owned done/archived runs only; in-flight runs steer-only (prompt.submit queues mid-turn → dual-driver risk); first message re-opens/links a board task (governance v3 visibility). Risk 3/10, complexity 4/10 Ally-scoped; 5-6/10 for specialists (D-B1 + F24 interplay — defer specialists with F24). | Post-Phase-8 |
|| F26 | ~~Floating navigation concierge~~ **DONE 2026-08-30** (t_50b4c29c; spike `docs/spike-concierge-widget-2026-08-30.md`) — BrainGlyph FAB bottom-right → panel docked right (minimizable pill, z-30 under drawers/toasts); chat = Ally 'concierge' LANE (second session on the default profile, persistent per browser, D-B1 untouched); first message carries compact nav brief + current page inside `[concierge-context v1]` markers (stripped for display), later messages page-only; BrainGlyph extracted to shared component; mock lanes keyed by agentId; lazy subscribe/rehydrate per open. Gotcha found: effect gated on a flag in its own deps array kills its subscription (the flag's setState triggers cleanup). v2 candidate: sidecar-grounded answers (+1 complexity). 227 vitest green. **New-chat control added 2026-08-30 (t_09cc51e8, Don's ask):** header ↺ = "Start a new concierge chat" — `startNewAssistantChat(agentId)` generalized across interface/live/mock (default lane keeps greeting, other lanes reset empty so the example chips resurface); next message after a clear carries the full nav brief again; live-probed against :9119 (`scripts/verify-concierge-newchat.mjs`) + :5173/:5200 serving checks. 228 vitest green | — |
||| F27 | **"Connected" browser pane** (Don idea 2026-08-30) | Launch a browser session from EAiOS that renders live in a right-side panel — watch the agent browse/work without leaving the console. Open questions: surface tech (VNC/noVNC stream vs. CDP screencast frames vs. screenshot polling), which browser backend (computer-use driver vs. headless Chromium), interaction model (read-only watch vs. click-through), and where it docks (Assistant rail vs. global right panel). Risk 4/10 (streaming plumbing, no product logic), complexity 7/10. ~2-3 sessions. | Post-beta |
||| F28 | **Recommendations page** (Don idea 2026-08-30) | A dedicated page for a weekly Ally-written post on how Don can use EAiOS more productively, grounded in observed work patterns (kanban history, chat topics, usage data, session activity). Delivery: Friday cron on Ally's profile that synthesizes the week and writes the post to an EAiOS-owned store; page lists posts newest-first. v1 = cron + storage + simple page; later: trend charts, per-agent breakdowns. Risk 2/10 (read-only synthesis, no external writes), complexity 4/10. ~1-2 sessions. | Post-beta |
||| F29 | ~~Spend watchdog~~ **DONE 2026-09-04** (Don-approved 2026-08-30, threshold **$5.00/day** set by Don 2026-09-04) — no-agent cron `d0f0f4fb3ba9` daily 06:30 → telegram portal: `scripts/spend-watchdog.py` (repo) copied to `~/.hermes/scripts/`; yesterday's spend across default + all profile DBs, per-model rate card (K3 $3/$14, K2.x $0.95/$4, DS-v4-flash $0.088/$0.176, Qwen3.7-flash $0.03/$0.13; fresh in+out only, cache excluded — validated vs Don's actual bill: Sep 2 estimate $11.35 vs ~$12 charged); alert carries top-3 sessions; SILENT when under threshold; env overrides SPEND_THRESHOLD_USD/SPEND_DAY_OFFSET for tests. 3/3 local tests + manual cron fire verified. **In-app DONE 2026-09-04:** shared `config/rate-card.json` (app + watchdog single source; watchdog also reads threshold from `settings.local.json dailySpendAlertUsd`); `/api/usage/daily` (apiCore `queryDailySpend` — all profile DBs, local-day buckets, zero-fill, top-3 sessions; shared router like /api/kanban + vite delegate passthrough); `DailySpendReport` domain + `getDailySpend`/`setDailySpendAlert` adapters (live degraded-tracking + mock parity); runtime `dailySpend` slice; Usage page "Daily estimated spend" card (14-day bars, risk-colored breaches, today dashed, day→top-sessions drill-down, threshold editor — same settings store the cron reads); `dailySpendAlertUsd` allowlisted in BOTH settings middlewares. Live-verified: endpoint reproduces the Sep-2 audit exactly ($11.35, "Casual check-in" $5.96 top); settings PUT round-trip on :5200; 234 vitest + build green | — |
||| F30 | **Tiered model routing** (Don 2026-08-30) | Day-to-day + cron jobs → cheapest capable model (Qwen/DeepSeek/K2.x-class); dev profile → K3/Codex for real work; Ally (default) decision pending — see session notes. Needs per-cron model override or per-profile pinning verified. **IN PROGRESS 2026-09-04:** OpenRouter key live (user-added to `~/.hermes/.env`; verified — DeepSeek V4 Flash round-trip $0.0000079; `hermes -z` via `--provider openrouter` OK; 427 models visible). Services restarted to re-read .env (eaios-hermes-serve + hermes-gateway; .env is file-loaded at startup, NOT inherited env). All 3 weekday-7am crons pinned `openrouter/deepseek-v4-flash` via `cron edit` (~$0.55→~$0.02/morning; also isolates briefings from Moonshot balance suspensions like the 2026-09-04 07:00 failures). Global fallback chain set: kimi-k3 → openrouter deepseek-v4-flash (`hermes config set` — config.yaml is agent-write-protected). Remaining: Ally → K2.7 Code trial at a session break; floor-model tool-use quality check on first live cron run. | Post-watchdog |
|||||||| F31 | ~~Flight booking + reservations~~ **DONE 2026-09-04** — left-nav Travel page with top cards (Flights, Hotels & Cars, Restaurants → search drawers), Upcoming trips list + per-trip TripDrawer with tabs (Itinerary/Confirmations/Approvals). Mock-first: TravelTrip/TravelBooking/TravelApproval domain types, TravelAdapter on HermesAdapter interface, MockTravelAdapter with fixtures (Baton Rouge trip + NYC dinner), LiveTravelAdapter → /api/travel/* server endpoints (503 → mock fallback). Server-side `travel.ts` pivoted from Amadeus (decommissioned July 2026) to a provider-agnostic router: Duffel for flights (`DUFFEL_API_KEY` — **configured and live-verified 2026-09-04**) is the only live provider in beta. **Browser-automation booking ARCHIVED 2026-09-09 for post-beta:** `server/travelBrowser/` moved to `archive/server-travelBrowser-2026-09-09/`; skipped tests and design docs moved to `archive/`. Travel page now shows an honest "Live hotel, car, and restaurant booking is post-beta" banner; the credential-vault drawer is removed. Ask Ally natural-language assistant remains. 266 vitest + build green. | Post-beta |
|||| F32 | **Telephone number to call Hermes/EAiOS** (Don 2026-09-04) | PSTN/SIP inbound voice requires a telephony provider (Twilio/Telnyx/Vonage), STT, TTS, and a voice pipeline integrated with the existing messaging gateway. Significant infrastructure; treat as post-beta roadmap. Risk 4/10, complexity 7/10. | Post-beta |

## Open design decisions (need the executive)

- **D-open-1 (F2):** should the CEO be able to disable an agent's skill from the dashboard, or is the library strictly read-only + request-via-delegation?
- ~~D-open-2: playbook edit UI — simple create/edit drawer, or stay file+git?~~ **RESOLVED by D-B3 (2026-08-27):** drawer, built in W7.

## Process rule adopted 2026-08-26

The original §-numbered product spec lived only in chat sessions; 5.5 had to
recover §8.7/§8.8 from session history. From now on every phase brief lands
in `~/eaios/docs/` BEFORE the phase starts, and this register is updated in
the same commit as the phase it affects.
